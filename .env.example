# Server-only secrets. Never prefix these with VITE_ and never expose them to browser code.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
SERPAPI_KEY=
JSEARCH_API_KEY=
YOUTUBE_API_KEY=

# Optional production server settings.
PORT=3000
NODE_ENV=production
